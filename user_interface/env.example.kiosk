# PhytoPi Dashboard - Kiosk Development Environment
# ==================================================
# For kiosk mode (Linux/Raspberry Pi)
# Copy to .env.kiosk or .env.local

# Supabase Configuration
# Same machine: Use localhost
# Remote device (Raspberry Pi): Use your computer's IP address
# Replace 192.168.0.107 with your computer's IP address
# Find your IP: ip addr show | grep "inet " | grep -v 127.0.0.1
SUPABASE_URL=http://192.168.0.107:54321
# For same machine: SUPABASE_URL=http://127.0.0.1:54321

SUPABASE_ANON_KEY=your-local-anon-key-here

# Get your local anon key:
# cd infra/supabase && supabase status

# Kiosk Mode
KIOSK_MODE=true

# Requirements:
# - Remote device and computer on the same network (for remote device)
# - Firewall allows port 54321
# - Supabase running locally: cd infra/supabase && supabase start

# Usage:
# ./scripts/build/build_kiosk.sh

